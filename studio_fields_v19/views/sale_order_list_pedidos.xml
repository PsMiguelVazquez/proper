<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        MIGRACIÓN V19: formaliza la personalización de Odoo Studio del menú
        "Contabilidad/Ventas/Pedidos de ventas" (`studio_customization.
        pedidos_de_ventas...` + su vista de lista sin xmlid). Mismo criterio
        que `sale_order_list_cotizaciones.xml`: vista de lista completa en
        vez de xpaths, referenciada explícitamente desde la acción.

        No incluye `x_lines_proposa` (subsistema de Requerimientos/
        Propuestas, fuera de alcance).
    -->
    <record id="sale_order_list_pedidos" model="ir.ui.view">
        <field name="name">sale.order.list.pedidos</field>
        <field name="model">sale.order</field>
        <field name="arch" type="xml">
            <list string="Quotations" class="o_sale_order" sample="1" decoration-muted="state == 'cancel'">
                <field name="message_needaction" column_invisible="True"/>
                <field name="currency_id" column_invisible="True"/>
                <field name="states_proposals" widget="many2many_tags"/>
                <field name="x_studio_comentarios" optional="hide"/>
                <field name="x_estado_compra"/>
                <field name="x_aprovacion_compras" invisible="1"/>
                <field name="validity_date" invisible="1"/>
                <field name="name" string="Number" readonly="1" decoration-bf="1"/>
                <field name="x_studio_n_orden_de_compra" optional="show"/>
                <field name="create_date" string="Creation Date" optional="show"/>
                <field name="commitment_date" optional="hide"/>
                <field name="expected_date" optional="hide"/>
                <field name="partner_id" readonly="1"/>
                <field name="user_id" widget="many2one_avatar_user" optional="show"/>
                <field name="activity_ids" widget="list_activity" optional="show"/>
                <field name="team_id" optional="hide"/>
                <field name="company_id" groups="!base.group_multi_company" column_invisible="True"/>
                <field name="company_id" groups="base.group_multi_company" optional="show" readonly="1"/>
                <field name="amount_untaxed" sum="Total Tax Excluded" widget="monetary" optional="hide"/>
                <field name="amount_tax" sum="Tax Total" widget="monetary" optional="hide"/>
                <field name="amount_total" sum="Total Tax Included" widget="monetary" decoration-bf="1"
                       decoration-info="invoice_status == 'to invoice'" optional="show"/>
                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" optional="hide"/>
                <field name="state" decoration-success="state == 'sale'" decoration-info="state == 'draft'"
                       decoration-primary="state == 'sent'" widget="badge" optional="show"/>
                <field name="x_estado_factura" optional="show"/>
                <field name="fecha_factura" optional="show"/>
                <field name="invoice_status" decoration-success="invoice_status == 'invoiced'"
                       decoration-info="invoice_status == 'to invoice'" decoration-warning="invoice_status == 'upselling'"
                       widget="badge" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="sale_order_action_pedidos" model="ir.actions.act_window">
        <field name="name">Pedidos de ventas</field>
        <field name="res_model">sale.order</field>
        <field name="view_mode">list,form,calendar,pivot,graph,activity</field>
        <field name="view_id" ref="sale_order_list_pedidos"/>
    </record>
</odoo>
