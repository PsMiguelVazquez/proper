<?xml version="1.0"?>
<odoo>


    <record id="action_refactura_credito" model="ir.actions.server">
        <field name="name">Refacturación (cŕedito)</field>
        <field name="type">ir.actions.server</field>
        <!-- MIGRACIÓN V19: `refacturacion.model_account_move` no existe (este
             módulo no define ese modelo, solo lo hereda); el xmlid correcto
             del ir.model de account.move es el del módulo que lo define. -->
        <field name="model_id" ref="account.model_account_move"/>
        <field name="binding_model_id" ref="account.model_account_move" />
        <field name="code">action=model.refactura_credito()</field>
        <field name="state">code</field>
    </record>



</odoo>
